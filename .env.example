# Copy this file to .env.local (for local dev) or set these as environment
# variables in your hosting provider's dashboard (for production).

# Signs login session cookies. Must be a long, random, secret string.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-this-to-a-long-random-value
